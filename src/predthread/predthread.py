from .reddit import open_reddit, open_thread 
import pandas as pd
from praw.reddit import Reddit

def open_standings_table(reddit: Reddit, url: str) -> pd.DataFrame:
    pass

def get_predictions(reddit: Reddit, url: str) -> pd.DataFrame:
    pass